'use strict'

// this has a linter error because we haven't written any functions yet

const app = require('./../app')
// functions that are API calls

// these are from /auth/api.js but might be a good starting point

// const post = function (data) {
//   return $.ajax({
//     url: app.host + '/sign-up',
//     method: 'POST',
//     data
//   })
// }

// const postSignIn = function (data) {
//   return $.ajax({
//     url: app.host + '/sign-in',
//     method: 'POST',
//     data
//   })
// }

module.exports = {
// put whatever we need to export here
// if it were the same as the commented code it would be
// post,
// postSignIn
}
